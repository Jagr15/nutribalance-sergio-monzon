drop function if exists public.registrar_orden_expedicion(
  uuid,
  uuid,
  text,
  numeric,
  text,
  text
);

drop function if exists public.registrar_orden_expedicion(
  uuid,
  uuid,
  text,
  numeric,
  numeric,
  text,
  text
);
